# Nimiq JSON-RPC endpoint used to verify receipt-bound settlement transactions.
# The endpoint must support `getTransactionByHash` and browser CORS.
# Choose a trusted endpoint for production and do not commit credentials here.
VITE_NIMIQ_RPC_URL=https://your-trusted-nimiq-rpc.example
